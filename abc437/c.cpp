#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using ll = long long;
using ull = unsigned long long;

int main() {
    int t;
    cin >> t;
    for(int i=0; i<t; ++i) {
        int n;
        cin >> n;
        vector<vector<ll>> ton (n, vector<int>(2));
        for(int j=0; j<n; ++j) {
            cin >> ton[j][0] >> ton[j][1];
        }
        
        vector<pair<int, 
    }
}